# ── Dev profile ─────────────────────────────────────────────────────────────
spring.config.activate.on-profile=dev

transit.offline.enabled=false

# MTA public endpoints don't require a key
transit.api.mta.api-key=
transit.api.transitland.api-key=dev-transitland-key

logging.level.com.transport.tracker=DEBUG